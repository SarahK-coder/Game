
        document.addEventListener('click', () => {
            if (this.gamePaused === false && this.bullets.length < 1) {
                const newBullet = new Bullet();
                this.bullets.push(newBullet);
            }
        });


///



class Game{

    start(){
        //...
        this.attachGameEventListeners();
    }

    attachGameEventListeners(){
        document.addEventListener('keypress', this.movePlayer);
        document.addEventListener('click', this.shootBullet);
    }

    cancelGameEventListners(){
        document.removeEventListener("keypress", this.movePlayer);
        document.removeEventListener("click", this.shootBullet);
    }


}


______

Bonus:
- refactor removeBombIfOutside() & removeBulletIfOutside() so that it receives the index of the Element ---> Tried this but it didn't work!

const gameElm = document.getElementById("game");